# ── Raw data (too large for GitHub) ──────────────────────────────────────────
data/raw/

# ── SQLite database (regenerated by pipeline — not committed) ─────────────────
data/processed/*.db

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Environment variables ─────────────────────────────────────────────────────
.env

# ── VS Code ───────────────────────────────────────────────────────────────────
# settings.json is intentionally committed (see .vscode/settings.json)
.vscode/extensions.json
.vscode/*.log

# ── Module outputs that are regenerated ───────────────────────────────────────
module2_analytics/charts/
module4_models/outputs/
module5_planning/outputs/

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
# NigeriaAgriScope deployment rules
data/processed/nigeria_agri.db
venv/
.venv/

# Force-include cloud fallback files
!data/processed/master_table.csv
!module4_models/yield_model.pkl
!module4_models/outputs/
!module5_planning/outputs/

# NigeriaAgriScope deployment rules

# Force-include cloud fallback files
!module4_models/outputs/yield_model.pkl
